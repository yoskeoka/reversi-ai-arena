# Rust Reference Player

This crate owns the Phase 2 mainline Rust AI-player path for
`reversi-ai-arena`.

It builds as a `wasm32-wasip1` command-style program and speaks the
`stdio-jsonrpc-ndjson` player contract through a sidecar manifest.
