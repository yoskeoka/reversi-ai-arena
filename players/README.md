# Player Surfaces

`players/` owns AI-player implementations and reference bots.

- `rust-reference/` is the mainline competitive lane.
- `go-reference/` is reserved for lightweight protocol samples and fixture
  support.
- `fixture-bots/` owns the deterministic local subprocess players used by
  Phase 1 tagged-runner verification.
