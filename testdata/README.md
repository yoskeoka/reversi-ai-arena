# Test Data

`testdata/` stores deterministic fixtures, replay artifacts, and sample match
inputs that are shared across verification layers.
`testdata/` owns deterministic fixture assets that are consumed by tests and
verification helpers.

Phase 1 stores the fixed scripted Reversi completion lines here.
